spring:
  application:
    name: studentlifejava

    jwt-secret: SPRING_JWT_SECRET
    # 30d
    refresh-token-expire: SPRING_REFRESH_TOKEN_EXPIRE
    # 15min
    access-token-expire: SPRING_ACCESS_TOKEN_EXPIRE
